route:
  receiver: unconfigured-operations-receiver
  group_by:
    - alertname
    - severity
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: unconfigured-operations-receiver
